
export const formatIndex = (str) => {
    if (!str) return ''
    return `(${str})`;
}
export const formatIndex1 = (str) => {
  if (!str) return ''
  return `[${str}]`;
}
export const formatIndex2 = (str) => {
  if (!str) return ''
  return `{${str}}`;
}
